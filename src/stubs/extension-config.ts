/**
 * Stub for workbench/extensionConfig — minimal types needed by lisp/types.ts.
 */

export interface FieldDefinition {
  name: string;
  type: string;
  offset?: number;
  size?: number;
  description?: string;
}
